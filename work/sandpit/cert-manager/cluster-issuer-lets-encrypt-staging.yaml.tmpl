---
###################################################################################################

kind: ClusterIssuer
apiVersion: {{.Env.CERT_MANAGER_CUSTOM_RESOURCE_DEFINITION_NAME}}/{{.Env.CERT_MANAGER_CUSTOM_RESOURCE_DEFINITION_VERSION}}
metadata:
  name: cluster-issuer-lets-encrypt-staging
spec:
  acme:
    email: {{.Env.CERT_MANAGER_EMAIL}}
    server: https://acme-staging-{{.Env.CERT_MANAGER_ACME_VERSION}}.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: cluster-issuer-lets-encrypt-staging
    dns01:
      providers:
        - name: route53
          route53:
            region: {{.Env.CERT_MANAGER_AWS_REGION}}
            accessKeyID: {{.Env.CERT_MANAGER_AWS_ACCESS_KEY}}
            secretAccessKeySecretRef:
              name: cert-manager
              key: aws_secret_key

###################################################################################################
...

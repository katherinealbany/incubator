###################################################################################################

[[ -z "${MONITORING_NAME}" ]] && export MONITORING_NAME='prometheus'

###################################################################################################

[[ -z "${MONITORING_COREDNS}" ]] && export MONITORING_COREDNS='false'
[[ -z "${MONITORING_KUBEDNS}" ]] && export MONITORING_KUBEDNS='true'

###################################################################################################

[[ -z "${MONITORING_VERSION}" ]] && export MONITORING_VERSION='1.7.0'

###################################################################################################

[[ -z "${MONITORING_KUBEETCD}" ]] && export MONITORING_KUBEETCD='false'

###################################################################################################

[[ -z "${MONITORING_REPO}"      ]] && export MONITORING_REPO='stable'
[[ -z "${MONITORING_CHART}"     ]] && export MONITORING_CHART='prometheus-operator'
[[ -z "${MONITORING_RELEASE}"   ]] && export MONITORING_RELEASE='monitoring'
[[ -z "${MONITORING_NAMESPACE}" ]] && export MONITORING_NAMESPACE="${MONITORING_RELEASE}"

###################################################################################################

[[ -z "${MONITORING_INGRESS_CLASS}" ]] && export MONITORING_INGRESS_CLASS='nginx'

###################################################################################################

[[ -z "${MONITORING_KUBESCHEDULER}" ]] && export MONITORING_KUBESCHEDULER='false'

###################################################################################################

[[ -z "${MONITORING_KUBEDNS_NAMESPACE}" ]] && export MONITORING_KUBEDNS_NAMESPACE='kube-system'
[[ -z "${MONITORING_KUBELET_NAMESPACE}" ]] && export MONITORING_KUBELET_NAMESPACE='kube-system'

###################################################################################################

[[ -z "${MONITORING_EXTERNAL_DNS_PUBLIC}" ]] && export MONITORING_EXTERNAL_DNS_PUBLIC='false'

###################################################################################################

[[ -z "${MONITORING_KUBECONTROLLERMANAGER}" ]] && export MONITORING_KUBECONTROLLERMANAGER='false'

###################################################################################################

[[ -z "${MONITORING_SERVICE_SESSION_AFFINITY}" ]] && export MONITORING_SERVICE_SESSION_AFFINITY='ClientIP'

###################################################################################################

[[ -z "${MONITORING_CERTMANAGER_CLUSTER_ISSUER}"      ]] && export MONITORING_CERTMANAGER_CLUSTER_ISSUER='cluster-issuer-lets-encrypt-staging'
[[ -z "${MONITORING_CERTMANAGER_ACME_CHALLENGE_TYPE}" ]] && export MONITORING_CERTMANAGER_ACME_CHALLENGE_TYPE='dns01'
[[ -z "${MONITORING_CERTMANAGER_ACME_DNS01_PROVIDER}" ]] && export MONITORING_CERTMANAGER_ACME_DNS01_PROVIDER='route53'

###################################################################################################

[[ -z "${MONITORING_CUSTOM_RESOURCE_DEFINITION_NAME}" ]] && export MONITORING_CUSTOM_RESOURCE_DEFINITION_NAME='monitoring.coreos.com'

###################################################################################################

[[ -z "${MONITORING_NODE_EXPORTER_RESOURCE_MEMORY}"      ]] && export MONITORING_NODE_EXPORTER_RESOURCE_MEMORY='100Mi'
[[ -z "${MONITORING_NODE_EXPORTER_RESOURCE_CPU_MIN}"     ]] && export MONITORING_NODE_EXPORTER_RESOURCE_CPU_MIN='5m'
[[ -z "${MONITORING_NODE_EXPORTER_RESOURCE_CPU_MAX}"     ]] && export MONITORING_NODE_EXPORTER_RESOURCE_CPU_MAX='10m'

[[ -z "${MONITORING_NODE_EXPORTER_ROOT_MOUNT_NAME}"      ]] && export MONITORING_NODE_EXPORTER_ROOT_MOUNT_NAME='root'
[[ -z "${MONITORING_NODE_EXPORTER_ROOT_MOUNT_HOSTPATH}"  ]] && export MONITORING_NODE_EXPORTER_ROOT_MOUNT_HOSTPATH='/'
[[ -z "${MONITORING_NODE_EXPORTER_ROOT_MOUNT_READONLY}"  ]] && export MONITORING_NODE_EXPORTER_ROOT_MOUNT_READONLY='true'
[[ -z "${MONITORING_NODE_EXPORTER_ROOT_MOUNT_MOUNTPATH}" ]] && export MONITORING_NODE_EXPORTER_ROOT_MOUNT_MOUNTPATH='/node/root'

###################################################################################################

[[ -z "${MONITORING_KUBE_STATE_METRICS_RESOURCE_MEMORY}"  ]] && export MONITORING_KUBE_STATE_METRICS_RESOURCE_MEMORY='100Mi'
[[ -z "${MONITORING_KUBE_STATE_METRICS_RESOURCE_CPU_MIN}" ]] && export MONITORING_KUBE_STATE_METRICS_RESOURCE_CPU_MIN='5m'
[[ -z "${MONITORING_KUBE_STATE_METRICS_RESOURCE_CPU_MAX}" ]] && export MONITORING_KUBE_STATE_METRICS_RESOURCE_CPU_MAX='10m'

###################################################################################################

[[ -z "${MONITORING_PROMETHEUS_OPERATOR_RESOURCE_MEMORY}"  ]] && export MONITORING_PROMETHEUS_OPERATOR_RESOURCE_MEMORY='100Mi'
[[ -z "${MONITORING_PROMETHEUS_OPERATOR_RESOURCE_CPU_MIN}" ]] && export MONITORING_PROMETHEUS_OPERATOR_RESOURCE_CPU_MIN='10m'
[[ -z "${MONITORING_PROMETHEUS_OPERATOR_RESOURCE_CPU_MAX}" ]] && export MONITORING_PROMETHEUS_OPERATOR_RESOURCE_CPU_MAX='50m'

###################################################################################################

[[ -z "${MONITORING_GRAFANA}"                                 ]] && export MONITORING_GRAFANA='true'
[[ -z "${MONITORING_GRAFANA_WORD}"                            ]] && export MONITORING_GRAFANA_WORD='grafana'

[[ -z "${MONITORING_GRAFANA_RESOURCE_MEMORY}"                 ]] && export MONITORING_GRAFANA_RESOURCE_MEMORY='750Mi'
[[ -z "${MONITORING_GRAFANA_RESOURCE_CPU_MIN}"                ]] && export MONITORING_GRAFANA_RESOURCE_CPU_MIN='100m'
[[ -z "${MONITORING_GRAFANA_RESOURCE_CPU_MAX}"                ]] && export MONITORING_GRAFANA_RESOURCE_CPU_MAX='250m'

[[ -z "${MONITORING_GRAFANA_ADMIN_USER}"                      ]] && export MONITORING_GRAFANA_ADMIN_USER='devops'
[[ -z "${MONITORING_GRAFANA_INGRESS_CLASS}"                   ]] && export MONITORING_GRAFANA_INGRESS_CLASS="${MONITORING_INGRESS_CLASS}"
[[ -z "${MONITORING_GRAFANA_DEFAULT_DASHBOARDS}"              ]] && export MONITORING_GRAFANA_DEFAULT_DASHBOARDS='false'
[[ -z "${MONITORING_GRAFANA_EXTERNAL_DNS_PUBLIC}"             ]] && export MONITORING_GRAFANA_EXTERNAL_DNS_PUBLIC="${MONITORING_EXTERNAL_DNS_PUBLIC}"

[[ -z "${MONITORING_GRAFANA_PERSISTENCE}"                     ]] && export MONITORING_GRAFANA_PERSISTENCE='true'
[[ -z "${MONITORING_GRAFANA_PERSISTENCE_SIZE}"                ]] && export MONITORING_GRAFANA_PERSISTENCE_SIZE='10Gi'
[[ -z "${MONITORING_GRAFANA_PERSISTENCE_ACCESS_MODE}"         ]] && export MONITORING_GRAFANA_PERSISTENCE_ACCESS_MODE='ReadWriteOnce'

[[ -z "${MONITORING_GRAFANA_SIDECAR_RESOURCE_MEMORY}"         ]] && export MONITORING_GRAFANA_SIDECAR_RESOURCE_MEMORY='150Mi'
[[ -z "${MONITORING_GRAFANA_SIDECAR_RESOURCE_CPU_MIN}"        ]] && export MONITORING_GRAFANA_SIDECAR_RESOURCE_CPU_MIN='1m'
[[ -z "${MONITORING_GRAFANA_SIDECAR_RESOURCE_CPU_MAX}"        ]] && export MONITORING_GRAFANA_SIDECAR_RESOURCE_CPU_MAX='10m'

[[ -z "${MONITORING_GRAFANA_CERTMANAGER_CLUSTER_ISSUER}"      ]] && export MONITORING_GRAFANA_CERTMANAGER_CLUSTER_ISSUER="${MONITORING_CERTMANAGER_CLUSTER_ISSUER}"
[[ -z "${MONITORING_GRAFANA_CERTMANAGER_ACME_CHALLENGE_TYPE}" ]] && export MONITORING_GRAFANA_CERTMANAGER_ACME_CHALLENGE_TYPE="${MONITORING_CERTMANAGER_ACME_CHALLENGE_TYPE}"
[[ -z "${MONITORING_GRAFANA_CERTMANAGER_ACME_DNS01_PROVIDER}" ]] && export MONITORING_GRAFANA_CERTMANAGER_ACME_DNS01_PROVIDER="${MONITORING_CERTMANAGER_ACME_DNS01_PROVIDER}"

([[ -n "${MONITORING_PERSISTENCE_STORAGE_CLASS}" ]] && [[ -z "${MONITORING_GRAFANA_PERSISTENCE_STORAGE_CLASS}" ]]) && export MONITORING_GRAFANA_PERSISTENCE_STORAGE_CLASS="${MONITORING_PERSISTENCE_STORAGE_CLASS}"

###################################################################################################

[[ -z "${MONITORING_PROMETHEUS}"                                       ]] && export MONITORING_PROMETHEUS='true'
[[ -z "${MONITORING_PROMETHEUS_WORD}"                                  ]] && export MONITORING_PROMETHEUS_WORD='prometheus'

[[ -z "${MONITORING_PROMETHEUS_RESOURCE_MEMORY}"                       ]] && export MONITORING_PROMETHEUS_RESOURCE_MEMORY='1250Mi'
[[ -z "${MONITORING_PROMETHEUS_RESOURCE_CPU_MIN}"                      ]] && export MONITORING_PROMETHEUS_RESOURCE_CPU_MIN='250m'
[[ -z "${MONITORING_PROMETHEUS_RESOURCE_CPU_MAX}"                      ]] && export MONITORING_PROMETHEUS_RESOURCE_CPU_MAX='500m'

[[ -z "${MONITORING_PROMETHEUS_REPLICAS}"                              ]] && export MONITORING_PROMETHEUS_REPLICAS='2'
[[ -z "${MONITORING_PROMETHEUS_INGRESS_CLASS}"                         ]] && export MONITORING_PROMETHEUS_INGRESS_CLASS="${MONITORING_INGRESS_CLASS}"
[[ -z "${MONITORING_PROMETHEUS_EXTERNAL_DNS_PUBLIC}"                   ]] && export MONITORING_PROMETHEUS_EXTERNAL_DNS_PUBLIC="${MONITORING_EXTERNAL_DNS_PUBLIC}"

[[ -z "${MONITORING_PROMETHEUS_PERSISTENCE_SIZE}"                      ]] && export MONITORING_PROMETHEUS_PERSISTENCE_SIZE='50Gi'
[[ -z "${MONITORING_PROMETHEUS_PERSISTENCE_ACCESS_MODE}"               ]] && export MONITORING_PROMETHEUS_PERSISTENCE_ACCESS_MODE='ReadWriteOnce'

[[ -z "${MONITORING_PROMETHEUS_RETENTION}"                             ]] && export MONITORING_PROMETHEUS_RETENTION='10d'
[[ -z "${MONITORING_PROMETHEUS_SCRAPE_INTERVAL}"                       ]] && export MONITORING_PROMETHEUS_SCRAPE_INTERVAL='15s'
[[ -z "${MONITORING_PROMETHEUS_POD_ANTI_AFFINITY}"                     ]] && export MONITORING_PROMETHEUS_POD_ANTI_AFFINITY='soft'
[[ -z "${MONITORING_PROMETHEUS_EVALUATION_INTERVAL}"                   ]] && export MONITORING_PROMETHEUS_EVALUATION_INTERVAL='15s'
[[ -z "${MONITORING_PROMETHEUS_SERVICE_MONITOR_SELECTOR}"              ]] && export MONITORING_PROMETHEUS_SERVICE_MONITOR_SELECTOR='prometheus: local'

[[ -z "${MONITORING_PROMETHEUS_CERTMANAGER_CLUSTER_ISSUER}"            ]] && export MONITORING_PROMETHEUS_CERTMANAGER_CLUSTER_ISSUER="${MONITORING_CERTMANAGER_CLUSTER_ISSUER}"
[[ -z "${MONITORING_PROMETHEUS_CERTMANAGER_ACME_CHALLENGE_TYPE}"       ]] && export MONITORING_PROMETHEUS_CERTMANAGER_ACME_CHALLENGE_TYPE="${MONITORING_CERTMANAGER_ACME_CHALLENGE_TYPE}"
[[ -z "${MONITORING_PROMETHEUS_CERTMANAGER_ACME_DNS01_PROVIDER}"       ]] && export MONITORING_PROMETHEUS_CERTMANAGER_ACME_DNS01_PROVIDER="${MONITORING_CERTMANAGER_ACME_DNS01_PROVIDER}"

[[ -z "${MONITORING_PROMETHEUS_POD_DISRUPTION_BUDGET}"                 ]] && export MONITORING_PROMETHEUS_POD_DISRUPTION_BUDGET='true'
[[ -z "${MONITORING_PROMETHEUS_POD_DISRUPTION_BUDGET_MAX_UNAVAILABLE}" ]] && export MONITORING_PROMETHEUS_POD_DISRUPTION_BUDGET_MAX_UNAVAILABLE='1'

([[ -n "${MONITORING_PERSISTENCE_STORAGE_CLASS}" ]] && [[ -z "${MONITORING_PROMETHEUS_PERSISTENCE_STORAGE_CLASS}" ]]) && export MONITORING_PROMETHEUS_PERSISTENCE_STORAGE_CLASS="${MONITORING_PERSISTENCE_STORAGE_CLASS}"

###################################################################################################

[[ -z "${MONITORING_ALERTMANAGER}"                                       ]] && export MONITORING_ALERTMANAGER='true'
[[ -z "${MONITORING_ALERTMANAGER_WORD}"                                  ]] && export MONITORING_ALERTMANAGER_WORD='alertmanager'

[[ -z "${MONITORING_ALERTMANAGER_RESOURCE_MEMORY}"                       ]] && export MONITORING_ALERTMANAGER_RESOURCE_MEMORY='100Mi'
[[ -z "${MONITORING_ALERTMANAGER_RESOURCE_CPU_MIN}"                      ]] && export MONITORING_ALERTMANAGER_RESOURCE_CPU_MIN='10m'
[[ -z "${MONITORING_ALERTMANAGER_RESOURCE_CPU_MAX}"                      ]] && export MONITORING_ALERTMANAGER_RESOURCE_CPU_MAX='100m'

[[ -z "${MONITORING_ALERTMANAGER_REPLICAS}"                              ]] && export MONITORING_ALERTMANAGER_REPLICAS='3'
[[ -z "${MONITORING_ALERTMANAGER_DEFAULT_RULES}"                         ]] && export MONITORING_ALERTMANAGER_DEFAULT_RULES='true'
[[ -z "${MONITORING_ALERTMANAGER_INGRESS_CLASS}"                         ]] && export MONITORING_ALERTMANAGER_INGRESS_CLASS="${MONITORING_INGRESS_CLASS}"
[[ -z "${MONITORING_ALERTMANAGER_POD_ANTI_AFFINITY}"                     ]] && export MONITORING_ALERTMANAGER_POD_ANTI_AFFINITY='soft'
[[ -z "${MONITORING_ALERTMANAGER_EXTERNAL_DNS_PUBLIC}"                   ]] && export MONITORING_ALERTMANAGER_EXTERNAL_DNS_PUBLIC="${MONITORING_EXTERNAL_DNS_PUBLIC}"

[[ -z "${MONITORING_ALERTMANAGER_PERSISTENCE_SIZE}"                      ]] && export MONITORING_ALERTMANAGER_PERSISTENCE_SIZE='10Gi'
[[ -z "${MONITORING_ALERTMANAGER_PERSISTENCE_ACCESS_MODE}"               ]] && export MONITORING_ALERTMANAGER_PERSISTENCE_ACCESS_MODE='ReadWriteOnce'

[[ -z "${MONITORING_ALERTMANAGER_CERTMANAGER_CLUSTER_ISSUER}"            ]] && export MONITORING_ALERTMANAGER_CERTMANAGER_CLUSTER_ISSUER="${MONITORING_CERTMANAGER_CLUSTER_ISSUER}"
[[ -z "${MONITORING_ALERTMANAGER_CERTMANAGER_ACME_CHALLENGE_TYPE}"       ]] && export MONITORING_ALERTMANAGER_CERTMANAGER_ACME_CHALLENGE_TYPE="${MONITORING_CERTMANAGER_ACME_CHALLENGE_TYPE}"
[[ -z "${MONITORING_ALERTMANAGER_CERTMANAGER_ACME_DNS01_PROVIDER}"       ]] && export MONITORING_ALERTMANAGER_CERTMANAGER_ACME_DNS01_PROVIDER="${MONITORING_CERTMANAGER_ACME_DNS01_PROVIDER}"

[[ -z "${MONITORING_ALERTMANAGER_POD_DISRUPTION_BUDGET}"                 ]] && export MONITORING_ALERTMANAGER_POD_DISRUPTION_BUDGET='true'
[[ -z "${MONITORING_ALERTMANAGER_POD_DISRUPTION_BUDGET_MAX_UNAVAILABLE}" ]] && export MONITORING_ALERTMANAGER_POD_DISRUPTION_BUDGET_MAX_UNAVAILABLE='1'

([[ -n "${MONITORING_PERSISTENCE_STORAGE_CLASS}" ]] && [[ -z "${MONITORING_ALERTMANAGER_PERSISTENCE_STORAGE_CLASS}" ]]) && export MONITORING_ALERTMANAGER_PERSISTENCE_STORAGE_CLASS="${MONITORING_PERSISTENCE_STORAGE_CLASS}"

###################################################################################################

###################################################################################################

kind: PodDisruptionBudget
apiVersion: "{{ .Values.apiVersionPodDisruptionBudget }}"
metadata:
  name: "{{ template "elasticsearchIngestName" . }}"
  namespace: "{{ template "elasticsearchNamespace" . }}"
  labels:
    name: "{{ template "elasticsearchIngestName" . }}"
    chart: "{{ template "elasticsearchChart" . }}"
    release: "{{ template "elasticsearchRelease" . }}"
    heritage: "{{ template "elasticsearchHeritage" . }}"
spec:
  selector:
    matchLabels:
      name: "{{ template "elasticsearchIngestName" . }}"
  {{- if .Values.ingestPodMinAvailable }}
  minAvailable: {{ .Values.ingestPodMinAvailable }}
  {{- else }}
  maxUnavailable: {{ .Values.ingestPodMaxUnavailable }}
  {{- end }}

###################################################################################################
